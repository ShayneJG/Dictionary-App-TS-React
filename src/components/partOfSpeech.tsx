import { Meanings } from "../App";

type PartOfSpeechProps = {
  meaning: Meanings;
};

const PartOfSpeech: React.FC<PartOfSpeechProps> = (meanings) => {
  return <div></div>;
};

export default PartOfSpeech;
