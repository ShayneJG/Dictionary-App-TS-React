const Settings = () => {
  return <div></div>;
};

interface SettingsProps {
  setSettings: (val: object) => void;
}

const Dropdown = ({ setSettings }: SettingsProps) => {
  return <div></div>;
};

export default Settings;
